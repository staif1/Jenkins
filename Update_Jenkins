+-----------------------------Обновление версии Jenkins----------------------------------+

manage jenkins -> на верху будет написанно какая версия доступна -> клик по
активной правой кнопокой мыши ссылке download -> копируем ссылку на скачивание 

переходим в консоль -> cd /usr/share/jenkins -> mv jenjins.war jenkins.номер.версии.war
wget ссылка на скачинвание 
просто ложим в диреторию новый jenkons файл

sudo service jenkins restart - рестатрт jenkins

